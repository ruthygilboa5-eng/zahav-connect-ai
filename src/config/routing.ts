// Route persistence configuration
export const PERSIST_LAST_ROUTE = false;   // must be false in production
export const PERSIST_AUTH = true;          // keep session if needed, but don't force route